extends layout

block content
    h1= title

    ul
    each output in output_list
        li
            a(href=output.url) #{output.name}
    else
        li There are no outputs.

    p
        a(href='/catalog/academic_outputs/create') 添加项目